# 今日头条视频播放流 + 搜索模块 MVP 技术文档

> 声明：本文档中的分析、设计拆解、UML、流程图和排期均为 AI 辅助生成。

## Part 1：实现方式

### 1.1 总体实现思路

项目采用 Flutter + Riverpod 的分层实现方式，核心路径是：

```text
Page / Widget
  ↓
Coordinator
  ↓
ViewModel / Controller
  ↓
Service / Repository
  ↓
DataSource
```

页面层负责展示和交互；Coordinator 负责跨模块流程编排；ViewModel / Controller 负责状态托管；Repository 屏蔽数据来源；DataSource 对接 Mock、本地缓存、离线索引和向量检索。

### 1.2 核心模块


| 模块             | 实现方式                                                             | 关键职责                                  |
| -------------- | ---------------------------------------------------------------- | ------------------------------------- |
| feed           | `FeedPage` + `FeedViewModel` + `FeedPlaybackCoordinator`         | 内容流展示、分页、当前项管理、播放编排入口                 |
| player         | `PlayerController` + `PlayerState` + 播放器 Widgets                 | active 播放器、preload 播放器、播放意图、进度、清晰度和横屏 |
| search         | `SearchViewModel` + `searchResultsProvider` + `SearchRepository` | 搜索历史、搜索结果异步加载、结果回 Feed 定位             |
| recommendation | `RecommendationService` + `recommendationWordsProvider`          | 按当前内容派生推荐词                            |
| observability  | `PlaybackStartupMetrics` + DebugReport                           | 旁路采集起播、首帧、缓冲、预加载事件                    |
| performance    | `PerformanceService`                                             | 消费观测结果，统计优化效果                         |
| storage        | `StorageService` + `SearchHistoryService`                        | 搜索历史和轻量配置存储                           |
| data           | models / repositories / datasources / search_index               | 数据模型、仓库、数据源、离线索引和混合检索                 |


### 1.3 播放实现

播放链路由 Feed 和 Player 分工完成：

- `FeedViewModel` 只维护内容列表、分页状态、当前索引和搜索定位状态。
- `FeedPlaybackCoordinator` 根据当前 FeedItem 类型决定播放、停止、预加载和恢复。
- `PlayerController` 管理真实播放器实例，包括 active controller 和单槽位 preload controller。
- `PlayerState` 向 UI 暴露播放、初始化、缓冲、进度、清晰度、横屏渲染等状态。

预加载采用单槽位策略：只保留一个候选视频，候选选择优先考虑滑动方向；命中时将 preload controller promote 为 active controller。

### 1.4 搜索实现

搜索分为搜索历史和搜索结果两条链路：

- 搜索历史由 `SearchViewModel` 通过 `SearchHistoryService` 存取。
- 搜索结果由 `searchResultsProvider(keyword)` 派生，调用 `SearchRepository.searchVideos(keyword)`。
- 默认数据源为 `DenseSearchDataSource`，优先使用离线业务文档、向量召回和关键词融合排序。
- 搜索配置或离线产物不可用时，回退到 Mock 搜索数据源。

搜索结果点击后不直接操作 Feed 页面状态，而是调用 `FeedPlaybackCoordinator.handleSearchResultSelected()`，再由 `FeedViewModel.focusItemById()` 定位目标视频。

### 1.5 可观测与性能统计

Observability 是旁路能力：

- 记录 `feed_item_visible`、初始化、播放请求、实际播放、首帧、缓冲、preload hit / miss / promote 等事件。
- 所有事件绑定 session，内部处理迟到事件、重复事件和失败事件。
- 不参与播放调度，不控制播放器生命周期，不修改用户播放意图。

当前采样结果显示，preload only 相比 baseline 在 `startup_ms p90 / p95` 和 `first_frame_ms p90 / p95` 上明显下降。

## Part 2：UML 结构

### 2.1 模块关系图

```mermaid
classDiagram
  class FeedPage {
    +watch FeedState
    +dispatch user events
  }

  class FeedViewModel {
    +loadInitial()
    +loadMore()
    +setCurrentIndex(index)
    +focusItemById(itemId)
  }

  class FeedPlaybackCoordinator {
    +handleFeedCurrentChanged(index)
    +handleSearchResultSelected(item)
    +handleFeedCovered()
    +handleFeedUncovered()
  }

  class PlayerController {
    +playVideo(item)
    +preloadVideo(item)
    +switchQuality(item, quality)
    +pause()
    +resume()
  }

  class SearchViewModel {
    +loadHistories()
    +submitSearch(keyword)
    +clearHistories()
  }

  class SearchRepository {
    +searchVideos(keyword)
  }

  class RecommendationService {
    +matchWords(item)
  }

  class PlaybackStartupMetrics {
    +markFeedItemVisible()
    +markPreloadHit()
    +buildReport()
  }

  FeedPage --> FeedViewModel
  FeedPage --> FeedPlaybackCoordinator
  FeedPlaybackCoordinator --> FeedViewModel
  FeedPlaybackCoordinator --> PlayerController
  FeedPlaybackCoordinator --> PlaybackStartupMetrics
  PlayerController --> PlaybackStartupMetrics
  SearchViewModel --> SearchHistoryService
  SearchRepository --> SearchDataSource
  RecommendationService --> RecommendationRepository
```



### 2.2 数据模型结构

```mermaid
classDiagram
  class FeedItem {
    <<abstract>>
    +String id
    +FeedItemType type
    +String title
    +Author author
    +Statistics statistics
    +List~String~ tags
    +List~String~ recommendationWords
    +DateTime createdAt
  }

  class VideoFeedItem {
    +List~VideoSource~ videoSources
    +String coverUrl
    +Duration duration
    +String description
  }

  class ImageFeedItem {
    +List~String~ imageUrls
    +String description
    +String imageUrl
  }

  class VideoSource {
    +VideoQuality quality
    +String url
    +int width
    +int height
    +int bitrate
  }

  class Author {
    +String id
    +String name
    +String avatarUrl
  }

  class Statistics {
    +int likeCount
    +int commentCount
    +int favoriteCount
    +int shareCount
  }

  class SearchVideoDocument {
    +String videoId
    +String title
    +String summary
    +List~String~ keywords
  }

  class OfflineVideoInput {
    +String videoId
    +String title
    +String description
    +List~String~ tags
    +List~String~ recommendationWords
    +String transcript
  }

  FeedItem <|-- VideoFeedItem
  FeedItem <|-- ImageFeedItem
  VideoFeedItem --> VideoSource
  FeedItem --> Author
  FeedItem --> Statistics
```



### 2.3 Provider 结构

```mermaid
classDiagram
  class ProviderScope
  class feedViewModelProvider
  class playerControllerProvider
  class searchViewModelProvider
  class feedPlaybackCoordinatorProvider
  class searchResultsProvider
  class recommendationWordsProvider
  class playbackStartupMetricsProvider

  ProviderScope --> feedViewModelProvider
  ProviderScope --> playerControllerProvider
  ProviderScope --> searchViewModelProvider
  ProviderScope --> feedPlaybackCoordinatorProvider
  ProviderScope --> searchResultsProvider
  ProviderScope --> recommendationWordsProvider
  ProviderScope --> playbackStartupMetricsProvider

  feedPlaybackCoordinatorProvider --> feedViewModelProvider
  feedPlaybackCoordinatorProvider --> playerControllerProvider
  feedPlaybackCoordinatorProvider --> playbackStartupMetricsProvider
  searchResultsProvider --> searchRepositoryProvider
  recommendationWordsProvider --> recommendationServiceProvider
```



### 2.4 状态对象结构

```mermaid
classDiagram
  class FeedState {
    +List~FeedItem~ items
    +int currentIndex
    +bool isLoading
    +bool isLoadingMore
    +bool hasMore
    +int currentPage
    +String? error
    +int? pendingFocusedIndex
  }

  class PlayerState {
    +String? videoId
    +VideoQuality selectedQuality
    +bool isInitializing
    +bool isInitialized
    +bool isPlaying
    +bool wantsToPlay
    +bool isBuffering
    +Duration currentPosition
    +Duration duration
    +String? error
    +bool isLandscapeRendering
  }

  class SearchState {
    +String keyword
    +List~String~ histories
    +bool isLoading
    +bool isSearching
    +String? error
  }

  class PlaybackStartupSession {
    +int sessionId
    +String videoId
    +int feedIndex
    +DateTime createdAt
    +PlaybackStartupSessionStatus status
    +DateTime? feedItemVisibleAt
    +DateTime? controllerInitializeStart
    +DateTime? controllerInitializeEnd
    +DateTime? playRequestedAt
    +DateTime? actualPlayingAt
    +DateTime? firstFrameRenderedAt
    +bool preloadHit
    +bool preloadMiss
    +bool preloadPromotedToActive
    +Object? error
  }

  class PlaybackStartupBaselineReport {
    +DateTime reportAt
    +Duration bufferingWindow
    +int visibleItems
    +int validFirstFrameSamples
    +int validStartupSamples
    +int validInitializeSamples
    +int expiredSessions
    +int incompleteSessions
    +int initializeFailedSessions
    +int ignoredLateEvents
    +int preloadVisibleItems
    +int preloadHits
    +int preloadMisses
    +int preloadPromotedToActive
    +String preloadHitRateLabel
  }

  class PlaybackStartupMetrics {
    +buildReport()
    +markFeedItemVisible()
    +markFirstFrameRendered()
    +markPreloadHit()
    +markPreloadMiss()
    +markPreloadPromotedToActive()
  }

  FeedState --> FeedItem
  PlayerState --> VideoQuality
  PlaybackStartupMetrics --> PlaybackStartupSession
  PlaybackStartupMetrics --> PlaybackStartupBaselineReport
```



### 2.5 数据访问与搜索索引结构

```mermaid
classDiagram
  class FeedRepository {
    +fetchFeedItems(page, pageSize)
  }

  class FeedDataSource {
    <<interface>>
    +fetchFeedItems(page, pageSize)
  }

  class MockFeedDataSource

  class SearchRepository {
    +searchVideos(keyword)
  }

  class SearchDataSource {
    <<interface>>
    +searchVideos(keyword)
  }

  class DenseSearchDataSource
  class MockSearchDataSource

  class VideoDenseSearchService {
    +search(query)
  }

  class SearchEmbeddingService {
    <<interface>>
    +embed(text)
  }

  class OpenAICompatibleSearchEmbeddingService
  class DeterministicSearchEmbeddingService

  class SearchVectorStore {
    <<interface>>
    +searchSummaryEmbedding(queryEmbedding, limit)
    +upsertSummaryEmbedding(videoId, summaryEmbedding)
  }

  class SearchVectorResult {
    +String videoId
    +double score
  }

  class MilvusSearchVectorStore

  class SearchBusinessStore {
    <<interface>>
    +findAll()
    +findByVideoId(videoId)
    +upsert(document)
  }

  class JsonSearchBusinessStore

  class VideoSearchOfflineIndexer {
    +index(input)
  }

  class VideoSummaryGenerator {
    <<interface>>
    +generate(input)
  }

  class LocalVideoSummaryGenerator
  class FixedVideoSummaryGenerator

  FeedRepository --> FeedDataSource
  FeedDataSource <|.. MockFeedDataSource

  SearchRepository --> SearchDataSource
  SearchDataSource <|.. DenseSearchDataSource
  SearchDataSource <|.. MockSearchDataSource

  DenseSearchDataSource --> VideoDenseSearchService
  DenseSearchDataSource --> SearchBusinessStore
  DenseSearchDataSource --> MockSearchDataSource : fallback
  VideoDenseSearchService --> SearchEmbeddingService
  VideoDenseSearchService --> SearchVectorStore
  VideoDenseSearchService --> SearchVectorResult

  SearchEmbeddingService <|.. OpenAICompatibleSearchEmbeddingService
  SearchEmbeddingService <|.. DeterministicSearchEmbeddingService
  SearchVectorStore <|.. MilvusSearchVectorStore
  SearchBusinessStore <|.. JsonSearchBusinessStore
  VideoSummaryGenerator <|.. LocalVideoSummaryGenerator
  LocalVideoSummaryGenerator <|-- FixedVideoSummaryGenerator

  VideoSearchOfflineIndexer --> OfflineVideoInput
  VideoSearchOfflineIndexer --> VideoSummaryGenerator
  VideoSearchOfflineIndexer --> SearchEmbeddingService
  VideoSearchOfflineIndexer --> SearchVectorStore
  VideoSearchOfflineIndexer --> SearchBusinessStore
  VideoSearchOfflineIndexer --> SearchVideoDocument
```



### 2.6 播放器 preload 结构

```mermaid
classDiagram
  class FeedPlaybackCoordinator {
    +handleFeedCurrentChanged(index)
    +handleSearchResultSelected(item)
    +handleLandscapeRequested(item)
    +handleFeedCovered()
    +handleFeedUncovered()
  }

  class PlayerController {
    -VideoPlayerController? activeController
    -VideoPlayerController? preloadController
    -String? preloadVideoId
    -PreloadControllerStatus preloadStatus
    +playVideo(item)
    +preloadVideo(item)
    +disposePreload()
    +switchQuality(item, quality)
  }

  class VideoPlayerController {
    +initialize()
    +play()
    +pause()
    +seekTo(position)
    +dispose()
  }

  class PreloadControllerStatus {
    <<enum>>
    idle
    initializing
    preloaded
    failed
  }

  class PlaybackStartupMetrics {
    +markPreloadHit()
    +markPreloadMiss()
    +markPreloadPromotedToActive()
  }

  FeedPlaybackCoordinator --> PlayerController
  FeedPlaybackCoordinator --> PlaybackStartupMetrics
  PlayerController --> VideoPlayerController : active
  PlayerController --> VideoPlayerController : preload
  PlayerController --> PreloadControllerStatus
  PlayerController --> PlaybackStartupMetrics
```



## Part 3：主要流程图

### 3.1 App 启动与首屏播放

```mermaid
flowchart TD
  A[App 启动] --> B[ProviderScope 初始化]
  B --> C[进入 FeedPage]
  C --> D[FeedViewModel 加载第一页]
  D --> E[FeedState 更新 items]
  E --> F[FeedPage 渲染当前项]
  F --> G[FeedPlaybackCoordinator 处理当前项]
  G --> H{当前项类型}
  H -->|视频| I[PlayerController 初始化并播放]
  H -->|图片| J[停止播放器]
  I --> K[调度下一个视频预加载]
```



### 3.2 上下滑切换播放

```mermaid
flowchart TD
  A[用户上下滑动] --> B[PageView onPageChanged]
  B --> C[FeedViewModel.setCurrentIndex]
  C --> D[FeedState.currentIndex 更新]
  D --> E[FeedPage 监听当前项变化]
  E --> F[FeedPlaybackCoordinator.handleFeedCurrentChanged]
  F --> G{当前项是否视频}
  G -->|是| H[创建起播 session]
  H --> I[PlayerController.playVideo]
  I --> J{是否命中 preload}
  J -->|命中| K[promote preload 为 active]
  J -->|未命中| L[初始化 active controller]
  G -->|否| M[PlayerController.stop]
  K --> N[按滑动方向选择新 preload 候选]
  L --> N
  M --> N
```



### 3.3 搜索与结果回 Feed

```mermaid
flowchart TD
  A[进入搜索页] --> B[输入关键词]
  B --> C[SearchViewModel.submitSearch]
  C --> D[保存搜索历史]
  D --> E[进入 SearchResultPage]
  E --> F[searchResultsProvider keyword]
  F --> G[SearchRepository.searchVideos]
  G --> H[展示视频结果]
  H --> I[点击结果]
  I --> J[FeedPlaybackCoordinator.handleSearchResultSelected]
  J --> K[FeedViewModel.focusItemById]
  K --> L[FeedState currentIndex + pendingFocusedIndex]
  L --> M[FeedPage 跳转目标页]
  M --> N[PlayerController 播放目标视频]
```



### 3.4 预加载命中热切换

```mermaid
flowchart TD
  A[当前视频播放] --> B[Coordinator 选择 preload 候选]
  B --> C[延迟 120ms 调度 preload]
  C --> D[PlayerController.preloadVideo]
  D --> E[preload controller 初始化完成]
  E --> F[用户滑动到候选视频]
  F --> G[PlayerController.playVideo]
  G --> H{preload 是否有效}
  H -->|有效| I[mark preload_hit]
  I --> J[promote preload to active]
  J --> K[mark preload_promoted_to_active]
  H -->|无效| L[mark preload_miss]
  L --> M[重新初始化 active controller]
```



### 3.5 横屏播放

```mermaid
flowchart TD
  A[点击横屏按钮] --> B[FeedPlaybackCoordinator.handleLandscapeRequested]
  B --> C[确保当前视频已初始化并保持播放意图]
  C --> D[设置 landscape rendering]
  D --> E[进入 LandscapePlayerPage]
  E --> F[横屏 UI watch PlayerState]
  F --> G[用户返回竖屏]
  G --> H[handleLandscapeClosed]
  H --> I[恢复 portrait rendering]
  I --> J[回到 FeedPage 并保持播放]
```



### 3.6 观测与报告

```mermaid
flowchart TD
  A[Feed item visible] --> B[创建 startup session]
  B --> C[controller initialize start/end]
  C --> D[play requested]
  D --> E[actual playing]
  E --> F[first frame rendered]
  E --> G[buffering start/end]
  B --> H[preload hit/miss/promote]
  F --> I[PlaybackStartupMetrics 汇总]
  G --> I
  H --> I
  I --> J[PlaybackStartupBaselineReport]
  I --> K[DebugReport JSON]
```



## Part 4：工作拆分 + 排期

### 4.1 工作拆分


| 阶段  | 工作项         | 主要产出                                              |
| --- | ----------- | ------------------------------------------------- |
| W1  | 基础播放链路      | Flutter 工程、Riverpod、Mock 数据、Feed 内容流、基础播放器、搜索入口雏形 |
| W2  | preload 优化  | 单 preload 槽位、方向感知候选、命中 promote、快速滑动收敛、起播观测        |
| W3  | 混合检索 + 交付产物 | 搜索历史、搜索结果、离线索引、Embedding / 向量召回、融合排序、测试和文档        |


### 4.2 排期


| 周期    | 重点          | 验收标准                                                                          |
| ----- | ----------- | ----------------------------------------------------------------------------- |
| 第 1 周 | 基础播放链路      | App 可启动；Feed 可上下滑和分页；视频可播放、暂停、展示进度；图片卡不初始化播放器；搜索入口和推荐词入口可进入对应页面               |
| 第 2 周 | preload 优化  | 单 preload 槽位可初始化和释放；按滑动方向选择候选；命中后 promote 为 active；快速滑动不阻塞当前播放；可导出起播观测 report |
| 第 3 周 | 混合检索 + 交付产物 | 搜索历史和搜索结果链路闭环；离线业务文档、Embedding、向量召回和关键词融合排序可用；关键单元/集成测试通过；技术方案、技术文档和测试记录归档    |


