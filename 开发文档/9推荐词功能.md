已完成第 9 阶段：推荐词功能。

现在 FeedPage 当前内容底部会展示“相关搜索”推荐词，优先使用 `FeedItem.recommendationWords`，为空时走 mock 推荐词库兜底。点击推荐词会保存搜索词，并进入 `SearchResultPage` 展示对应搜索结果。

完成文件：

- `lib/features/recommendation/services/recommendation_service.dart`
- `lib/features/feed/widgets/related_search_entry.dart`
- `lib/data/repositories/recommendation_repository.dart`
- `lib/data/datasources/mock_recommendation_datasource.dart`
- `lib/features/recommendation/providers/recommendation_provider.dart`
- `lib/mock/mock_recommendations.dart`
- 接入了 `lib/features/feed/pages/feed_page.dart`

也补了测试：

- `test/recommendation_entry_test.dart`

验证通过：

- `flutter analyze`
- `flutter test`，6 个测试全部通过