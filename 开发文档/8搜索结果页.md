已完成第 8 阶段：搜索结果页。

做了这些：

- 新增 `SearchResultPage`，支持根据关键词展示视频搜索结果
- 新增 `SearchRepository` 和 `MockSearchDataSource`
- 本地搜索只匹配 `VideoFeedItem`，图片卡不会进入结果
- 搜索中间页提交关键词后会进入结果页
- 搜索结果项展示封面、标题、作者、点赞数
- 点击结果后回到 `FeedPage`，自动加载目标视频所在分页并定位播放
- FeedPage 接入 `PageController`，可跟随 `FeedViewModel.currentIndex` 跳转到指定内容

另外补了测试：

- 搜索历史保存流程仍可用
- 搜索“拉伸”能看到匹配视频
- 点击结果能回到 Feed 并定位到对应视频

验证通过：

- `flutter analyze`
- `flutter test`，5 个测试全部通过