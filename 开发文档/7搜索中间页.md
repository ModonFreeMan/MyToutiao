已完成第 7 阶段：搜索中间页。

主要完成了：

- FeedPage 顶部增加搜索入口，点击进入 `SearchMiddlePage`
- 新增搜索中间页输入框，支持键盘搜索/按钮提交
- 新增搜索历史状态与 `SearchViewModel`
- 新增 `StorageService` 和 `SearchHistoryService`
- 使用 `shared_preferences` 保存本地搜索历史
- 历史词展示、清空、点击历史词再次提交搜索
- 增加测试覆盖“输入搜索词 -> 保存历史 -> 返回再进入仍可看到历史”

涉及文件包括：

- `lib/features/search/pages/search_middle_page.dart`
- `lib/features/search/widgets/search_input_bar.dart`
- `lib/features/search/widgets/search_history_list.dart`
- `lib/features/search/states/search_state.dart`
- `lib/features/search/view_models/search_view_model.dart`
- `lib/features/storage/services/storage_service.dart`
- `lib/features/storage/services/search_history_service.dart`
- `lib/features/storage/providers/storage_provider.dart`
- `lib/features/feed/pages/feed_page.dart`
- `lib/app/app_router.dart`
- `pubspec.yaml`

验证结果：

- `flutter analyze` 通过
- `flutter test` 通过，4 个测试全部通过