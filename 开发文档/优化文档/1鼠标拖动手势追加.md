鼠标拖动手势。

我在 [lib/app/app.dart](</d:/Flutter/VideoPlayer/lib/app/app.dart>) 里给 `MaterialApp` 加了全局 `ScrollBehavior`，把 `PointerDeviceKind.mouse` 加进可拖动设备里。这样 `PageView`、`ListView`、横向滚动区域都可以用鼠标按住拖动了。

也补了测试：[test/widget_test.dart](</d:/Flutter/VideoPlayer/test/widget_test.dart>)，验证 Feed 页能通过鼠标拖拽切到下一页。