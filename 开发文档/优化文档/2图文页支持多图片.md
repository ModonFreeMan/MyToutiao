图文卡从单图升级成了“单个 Feed 页面内横向滑多图”：

- [image_feed_item.dart](d:/Flutter/VideoPlayer/lib/data/models/image_feed_item.dart)：`imageUrl` 改为 `imageUrls`，并保留 `imageUrl` getter 兼容旧引用。
- [mock_images.dart](d:/Flutter/VideoPlayer/lib/mock/mock_images.dart)：每条图文 mock 都改成 3 张图。
- [image_feed_card.dart](d:/Flutter/VideoPlayer/lib/features/feed/widgets/image_feed_card.dart)：图文区域改为内部横向 `PageView`，显示 `图文 1/3` 这类页码和底部分段指示器；渐变遮罩已设置为忽略手势，避免挡住横滑。
- 测试里把外层竖向 `PageView` 查找收紧，并新增了图文横滑测试。

原本mock数据是固定数量图片，已调整成更真实的变长图文数据了。
现在 [mock_images.dart](d:/Flutter/VideoPlayer/lib/mock/mock_images.dart) 里的图文图片数量不再统一，有 3 张、4 张、5 张混合。测试也改了：

- 页码断言按当前 mock item 的 `imageUrls.length` 动态计算
- 新增测试确保 mock 数据里确实存在不同图片数量，并包含 `3/4/5` 这几类